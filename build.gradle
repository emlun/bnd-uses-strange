apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'osgi'

sourceCompatibility = 1.5
version = '1.0'

jar {
    baseName = 'com.acme.el'
    manifest {
        symbolicName = baseName
    }
}
